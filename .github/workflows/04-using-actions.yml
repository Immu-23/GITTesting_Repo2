name: Java and Maven

on: push

jobs:
   echo_java_mavensetup:
     runs-on: ubuntu-latest
     steps: 
          - uses: actions/checkout@v4
          - uses: actions/setup-java@v4
            with:
             java-version: '17'
             distribution: 'temurin'
          - run: mvn --barch-mode --update.snapshots verify
          - run: mkdir stagging && target/*.jar stagging
          - uses: actions/upload-artifact@v4
            with:
              name: Latest Run
              path: stagging
